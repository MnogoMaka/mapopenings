services:
  razritiya:
    build: .
    container_name: razritiya-app
    ports:
      - "8050:8050"
    volumes:
      - ./result.json:/app/result.json:ro
    environment:
      - OAUTH_TOKEN=${OAUTH_TOKEN}
    restart: unless-stopped
